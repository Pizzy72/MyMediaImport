<Window x:Class="MyMediaImport.App.SourceFolderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose source folder" Width="620" Height="520"
        MinWidth="420" MinHeight="350" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" Loaded="Window_OnLoaded" Closed="Window_OnClosed">
    <Window.Resources>
        <Style TargetType="TreeViewItem" BasedOn="{StaticResource BaseControlStyle}">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ToggleButton x:Name="Expander" MinWidth="24" Focusable="False"
                                          IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent" Padding="4">
                                            <TextBlock x:Name="Glyph" Text="+"
                                                       Foreground="{DynamicResource ForegroundBrush}"
                                                       HorizontalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Glyph" Property="Text" Value="-" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Border x:Name="HeaderBorder" Grid.Column="1" Padding="6,4"
                                    Background="Transparent" CornerRadius="3">
                                <ContentPresenter x:Name="PART_Header" ContentSource="Header" />
                            </Border>
                            <ItemsPresenter x:Name="Children" Grid.Row="1" Grid.Column="1" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="False">
                                <Setter TargetName="Children" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource SelectionBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource SelectionForegroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Choose a folder. Its subfolders are included. No files are changed."
                   TextWrapping="Wrap" Margin="0,0,0,12" />
        <TreeView x:Name="FolderTree" Grid.Row="1"
                  SelectedItemChanged="FolderTree_OnSelectedItemChanged"
                  AutomationProperties.AutomationId="SourceFolderTree">
            <TreeView.Template>
                <ControlTemplate TargetType="TreeView">
                    <Border Background="{DynamicResource ControlBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="4">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                                      Focusable="False">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </TreeView.Template>
        </TreeView>
        <TextBlock x:Name="SelectedFolderText" Grid.Row="2" TextWrapping="Wrap" Margin="0,10,0,0" />
        <TextBlock x:Name="StatusText" Grid.Row="3" TextWrapping="Wrap"
                   Style="{StaticResource MutedTextStyle}" Margin="0,6,0,0"
                   AutomationProperties.AutomationId="SourceFolderStatus" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="ChooseButton" Content="Use this folder" IsDefault="True" IsEnabled="False"
                    Click="ChooseButton_OnClick" AutomationProperties.AutomationId="ConfirmSourceFolderButton" />
            <Button Content="Cancel" IsCancel="True" Margin="8,0,0,0" />
        </StackPanel>
    </Grid>
</Window>
